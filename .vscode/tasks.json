{
    "tasks": [
        {
            "label": "Build",
            "type": "shell",
            "command": "spwn",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "args": [
                "build", "main.spwn", "--level-name", "CTF CHALL 2022", "--allow", "readfile"
            ]
        }
    ]
}